# Architecture decision records

One file per hard-to-reverse decision, `NNNN-slug.md`, written by `/domain-modeling` (reached through `/grill-with-docs`) when a decision is actually made — see `docs/agents/domain.md`. Empty until then; the measured history of past decisions lives in `docs/traps/`.

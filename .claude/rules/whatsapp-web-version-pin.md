---
paths:
  - "client/api_patches/start.js"
---

# Whatsapp web version pin

**Read `docs/traps/whatsapp-web-version-pin.md` before changing these files.** Short form:

Interception and the pin are the same mechanism and must stay document-only. Never run unpinned. The catalogue expires; a newest build is not necessarily one wa-js can drive — compare sessions within one log before blaming the pin.
